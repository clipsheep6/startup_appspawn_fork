diff a/test/unittest/app_spawn_stub.cpp b/test/unittest/app_spawn_stub.cpp	(rejected hunks)
@@ -103,6 +103,13 @@ void DisallowInternet(void)
 {
 }
 
+int32_t SetInternetPermission(uint32_t uid, uint8_t allow)
+{
+    UNUSED(uid);
+    UNUSED(allow);
+    return 0;
+}
+
 int BindStub(int sockfd, const struct sockaddr *addr, socklen_t addrlen)
 {
     UNUSED(sockfd);
